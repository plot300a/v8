import { StyleSheet, Text, View } from "react-native";
import React from "react";
import { TrendsButtons } from "@/components/trends/header";
import ViralShowcase from "@/components/trends/viralShowcase";

export default function Trends() {
    return (
        <TrendsButtons />
    );
}

const styles = StyleSheet.create({
    buttonContainer: {},
});
